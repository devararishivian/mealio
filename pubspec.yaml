name: mealio
description: A meal catalogue app
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  http: ^0.12.0+2
  font_awesome_flutter: ^8.5.0
  sqflite: ^1.1.7
  path_provider: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: ^1.5.11
  mockito: ^4.1.1

flutter:

  uses-material-design: true
  assets:
    - images/